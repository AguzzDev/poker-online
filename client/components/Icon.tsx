export const IconSm = ({ Icon, color }: any) => {
  return <Icon className={`${color} h-8 w-8`} />
}
export const IconXs = ({ Icon, color }: any) => {
  return <Icon className={`${color} h-5 w-5`} />
}
